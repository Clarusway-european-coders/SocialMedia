import React from "react";
import BlobContainer, { BlobShape } from "./Blob.styled";

const Blob = () => {
  return (
    <BlobContainer>
      <BlobShape />
    </BlobContainer>
  );
};

export default Blob;
